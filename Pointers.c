/*
Pointers are data types themselves
A pointer is a data item whose:
• Value is a memory address
• Type describes the data located at that memory address


*x returns the data that lives at the memory address stored in the pointer x

&x extracts the value of the address of variable "x"
&arr[i] extracts the value of the address of arr[i]

/*
